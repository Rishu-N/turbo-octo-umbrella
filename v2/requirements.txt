# Core (pinned for reproducibility; versions matched to ../v1 where they overlap).
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.2          # logistic-regression / nearest-centroid head (Phase 5)
joblib==1.3.2                # persist the learned head (compatible with scikit-learn 1.4.2)
pyyaml==6.0.1                # config + intents loader

# Jina v3 embeddings API client — the ONLY network dependency on the hot path.
# Retries/backoff use urllib3's built-in Retry adapter, so no extra dependency is needed.
requests==2.31.0

# Tests
pytest==8.1.1

# --- Optional (config-gated; install only if you enable the feature) ----------------
# Vector store backend (default). To swap to faiss, `pip install faiss-cpu` and flip
# vector_store.backend in config.yaml — the store/ interface keeps it isolated.
hnswlib==0.8.0
# Romanized-Hindi -> Devanagari transliteration (normalization.transliterate_romanized: true).
indic-transliteration==2.3.51
# Optional FastAPI serving (src/serve.py, Phase 6).
fastapi==0.110.1
uvicorn==0.29.0
